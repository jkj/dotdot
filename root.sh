# Overrides specifically for the root user. This is sourced after common.sh so
# it can override any aliases, custom paths etc. Nothing in this file should
# be shell specific - it is sourced in for all Bourne-compatible shells.
